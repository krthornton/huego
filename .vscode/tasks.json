{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "launch headless delve",
            "type": "shell",
            "command": "dlv debug --headless --api-version=2 --listen=127.0.0.1:43000 .",
            "isBackground": true
        }
    ]
}